@model FlagFinder.Models.Flag

@{
    string header = Model.Name;
    if (Model.ShortName != "")
    {
        header += " (" + Model.ShortName + ")";
    }
    ViewBag.Title = "Flag - " + Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*<input id="countryName" type="hidden" value="@Model.Name" />*@
<h2>@header</h2>
<hr />
<div class="row">
    <div class="col-sm-3">
        <img src="@Model.ImageLocation" class="img-responsive" /><br />
        @*<br />*@
        @Html.ActionLink("Back", "Index")
        @*<a class="btn btn-default" href="/">Back</a>*@
    </div>
    <div class="col-sm-2">
        <h4>Colors</h4>
        <div class="[ btn-group ] img-responsive" >
            @if (Model.Red)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:red" class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Red
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Orange)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:orange" class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Orange
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Yellow)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:yell" class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Yellow
            </label>
        </div>

        <div class="[ btn-group ]">
            @if (Model.Green)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:green" class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Green
            </label>
        </div>

        <div class="[ btn-group ]">
            @if (Model.Blue)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:blue" class="[ glyphicon glyphicon-ok ]"></span>
                </label>                }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Blue
            </label>
        </div>

        <div class="[ btn-group ]">
            @if (Model.Purple)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:purple" class="[ glyphicon glyphicon-ok ]"></span>
                </label>                
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Purple
            </label>
        </div>

        <div class="[ btn-group ]">
            @if (Model.Brown)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:brown" class="[ glyphicon glyphicon-ok ]"></span>
                </label>                }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Brown
            </label>
        </div>

        <div class="[ btn-group ]">
            @if (Model.White)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:black" class="[ glyphicon glyphicon-ok ]"></span>
                </label>                }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                White
            </label>
        </div>

        <div class="[ btn-group ]">
            @if (Model.Black)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>               
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Black
            </label>
        </div>
    </div>
    <div class="col-sm-2">
        <h4>Shapes</h4>
        <div class="[ btn-group ]">
            @if (Model.Circle)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Circle
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Triangle)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Triangle
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Star)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Star
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Sun)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Sun
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Moon)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Moon
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.Cross)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                Cross
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.TriColorHorizontal)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                <img src="~/Content/Images/TriColorHorizontal.png" />
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.TriColorVertical)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                <img src="~/Content/Images/TriColorVertical.png" />
            </label>
        </div>
        <div class="[ btn-group ]">
            @if (Model.BiColorHorizontal)
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span class="[ glyphicon glyphicon-ok ]"></span>
                </label>
            }
            else
            {
                <label class="[ label-chkbox btn btn-default ]">
                    <span style="color:white" class="[ glyphicon glyphicon-ok  ]"></span>
                </label>
            }
            <label class="[ check-lbl btn active ]">
                <img src="~/Content/Images/BiColorHorizontal.png" />
            </label>
        </div>
    </div>
    <div class="col-sm-5 text-center" style="align-content: center">
        <div id="map" style="width:100%;height:320px;">
            @*<a class="btn btn-default" href="/">Home</a>*@
        </div>
    </div>
</div>

<script>
        function myMap() {
            //var mapOptions = {
            //    center: new google.maps.LatLng(51.5, -0.12),
            //    zoom: 3,
            //    mapTypeId: google.maps.MapTypeId.HYBRID
            //}
            var mapOptions = {
                zoom: 3
            }

            var map = new google.maps.Map(document.getElementById("map"), mapOptions);

            var address = "country @Model.Name";
            geocoder = new google.maps.Geocoder();
            geocoder.geocode({ 'address': address }, function (results, status) {
                if (status == 'OK') {
                    map.setCenter(results[0].geometry.location);
                    var marker = new google.maps.Marker({
                        map: map,
                        position: results[0].geometry.location
                    });
                } else {
                    alert('Geocode was not successful for the following reason: ' + status);
                }
            });
        }
</script>

<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBtKuxHmT5JXBE284JE9S_SzMqQvtWKwiE&callback=myMap"></script>

